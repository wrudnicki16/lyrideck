appId: host.exp.Exponent
---
# Journey 7: Spotify authentication
# Note: Full auth flow cannot be automated (requires Spotify credentials)
- launchApp

# Assert app launches with DeckImportScreen
- assertVisible: "Anki2Spotify"

# The auth bar in App.tsx shows a "Connect Spotify" or login button
# when not authenticated. The exact text depends on the auth state.

# Note: Spotify login requires entering credentials in a WebBrowser,
# which cannot be automated by Maestro. This flow verifies the app
# launches and the auth UI is present. Manual testing is required
# for the full OAuth PKCE flow.
